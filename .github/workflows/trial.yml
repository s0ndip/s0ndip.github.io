name: trial

on:
  workflow_dispatch: # Allows manual triggering

jobs:
  php-script:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout main repository
    - name: Checkout main repository
      uses: actions/checkout@v3

    # Step 2: Configure git username
    - name: Configure git username
      run: git config --global user.name "GitHub Actions"

    # Step 3: Clone the private repository
    - name: Clone private repository
      env:
        PRIVATE_REPO_PAT: ${{ secrets.copy_files }} # Token stored securely
      run: |
        git clone https://x-access-token:${copy_files}@github.com/s0ndip/epg-grabber.git epg-grabber
        cp epg-grabber/tempest.php ./tempest.php
        cp -r epg-grabber/tempest_config ./tempest_config

    # Step 4: Verify files
    - name: Verify `tempest.php` and `tempest_config`
      run: |
        ls -al
        if [ ! -f "tempest.php" ]; then
          echo "Error: tempest.php not found!";
          exit 1;
        fi
        if [ ! -d "tempest_config" ]; then
          echo "Error: tempest_config directory not found!";
          exit 1;
        fi

    # Step 5: Set up Git identity
    - name: Set up Git identity
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"

    # Step 6: Install PHP and required extensions
    - name: Setup PHP
      uses: shivammathur/setup-php@2.31.1
      with:
        php-version: 8.3
        extensions: xml, imagick, zip, curl
        ini-values: memory_limit=-1

    # Step 7: Verify PHP installation
    - name: Verify PHP installation
      run: php -v

    # Step 8: Run Tempest script
    - name: Run Tempest script
      run: |
        php tempest.php --epg config=test.config.xml invgz
        [ -f tempest_config/epg/tempest_with_details.xml ] && rm tempest_config/epg/tempest_with_details.xml
        [ -f tempest_config/epg/tempest.xml ] && rm tempest_config/epg/tempest.xml

    # Step 9: Check for changes and commit updated files
    - name: Check for changes
      run: |
        git add .
        if git diff --cached --quiet; then
          echo "No changes to commit.";
          exit 0;
        fi

    - name: Commit updated files
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: Automated epg update
